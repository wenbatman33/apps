# 正式素材接入盤點

## 已接入

- 背景：四段章節背景、Free Game 背景、四種結局背景。
- 角色：項羽、虞姬、劉邦 base character。
- 轉盤：`symbols/gpt_eastern_quality/` 12 顆正式符號。
- 主要 UI：`ui_reel_frame.png`、`ui_bottom_panel.png`。
- SPIN：`ui_spin_btn_normal.png`、`ui_spin_btn_hover.png`、`ui_spin_btn_pressed.png` 三態。
- Win Presentation：四種大獎背景、四種大獎標題圖、`text_prize_win_amount_cn.png`。
- 數字：`numbers/gold_3d_clean/` 已用於大獎金額。
- Art Bible 實機預覽：已改用正式 reel frame / bottom panel，不再使用 simplified revision。

## 待接入

- `ui_auto_button.png`：目前已載入，但尺寸與位置仍需依正式底板重排。
- `ui_buy_feature_button.png`：目前已載入，但左側位置仍像臨時入口，需要對齊正式版操作區。
- `ui_amount_plate.png`：應用於贏分、押注、餘額欄位，取代純文字直接壓在底板上。
- `ui_free_awaken_overlay_panel.png`：Free Game 觸發畫面應接入。
- `ui_win_result_overlay_panel.png`：一般結算蓋板應接入。
- `ui/multiplier/`：Free Game 倍數 HUD、倍數 popup、小倍數 badge 仍未正式接上。
- `bawangbieji/effects/coins/`：大獎金幣飛散仍未接上逐幀素材。
- `bawangbieji/effects/flame_drop/`、`fx_tumble_burst_sheet.png`：消除與掉落特效仍是程式畫線，需換成正式特效。
- `characters/{xiangyu,liubang,yuji}/`：角色技能演出已統一到三層內正式資料夾，後續只替換同名連續幀。
- `bawangbieji/cinematics/`：章節轉場與 Free Game 插入轉場仍未接入。

## 不應再使用

- `ui/revisions/ui_reel_frame_simplified.png`
- `ui/revisions/ui_bottom_panel_simplified.png`
- 程式手畫的主按鈕、轉盤外框、底部面板、Big Win 按鈕。

## 下一步順序

1. 重排底部操作區：SPIN、AUTO、BET、BUY FEATURE 全部按正式 UI 對位。
2. 將底部數字欄位改用 `ui_amount_plate.png` 與 `gold_3d_clean` 數字。
3. 接 Free Game 倍數 HUD 與倍數增加 popup。
4. 接消除、掉落、金幣、大獎粒子特效。
5. 精修角色技能連續幀與章節轉場動畫。
