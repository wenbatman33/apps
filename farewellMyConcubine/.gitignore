# 相依套件（用 npm install 還原）
node_modules/

# build 產物
dist/

# Claude Code 本地 worktree（scratch，不推 repo）
.claude/worktrees/

# 系統檔
.DS_Store
**/.DS_Store

# 美術原始庫（217MB：影片 / GPT 原始大圖 / 預覽 / temp，遊戲執行用不到）
# 檔案仍保留在本機，只是不進 git，避免 repo 永久肥大
assets/美術製作/
